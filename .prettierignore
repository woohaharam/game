dist/
node_modules/
coverage/
package-lock.json
docs/media/

# Generated by `npm run balance`. Reformatting a generated file would make the
# next regeneration show a spurious diff.
docs/BALANCE.md

# A separate project with its own Prettier and ESLint, and its own CI job.
# Formatting it from here would apply the wrong config and duplicate the check.
deepdelve/
